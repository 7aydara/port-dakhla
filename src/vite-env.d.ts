/// <reference types="vite/client" />

/** Empreinte du commit, injectee a la compilation (voir vite.config.ts). */
declare const __VERSION__: string;
/** Date de compilation, injectee a la compilation. */
declare const __CONSTRUIT_LE__: string;
